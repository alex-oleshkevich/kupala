[project]
name = "demo"
description = "Demo application exercising the framework."
requires-python = ">=3.14"
version = "0"
dependencies = [
    "kupala",
    "uvicorn>=0.52.1",
]

[tool.uv]
package = false
