# kupala-gen

Code generation for Kupala applications: project scaffolding, modules, and features.

The generators run through the `kupala` command line, so they are available wherever the CLI is:

```bash
uvx --from 'kupala[gen]' kupala gen --help
```
