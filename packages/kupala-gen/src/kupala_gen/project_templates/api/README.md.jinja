# {{ name }}

Run the API:

```console
uv run uvicorn {{ package }}.app:app --reload
```

OpenAPI is available at `http://127.0.0.1:8000/api/openapi.json`.

Run the tests:

```console
uv run pytest
```
