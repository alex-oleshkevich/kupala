import collections.abc

import pytest
from kupala import TestClient

from {{ package }}.app import app


@pytest.fixture
def client() -> collections.abc.Iterator[TestClient]:
    with TestClient(app) as client:
        yield client
